bash -lc 'OLD="3D84D5B8-7280-4133-96E9-B407AC0DD9AA"; echo "Killing Simulator..."; killall Simulator 2>/dev/null || true; xcrun simctl shutdown all 2>/dev/null || true; echo "Deleting broken simulator: $OLD"; xcrun simctl delete "$OLD" 2>/dev/null || true; rm -rf "$HOME/Library/Developer/CoreSimulator/Devices/$OLD"; killall com.apple.CoreSimulator.CoreSimulatorService 2>/dev/null || true; sleep 2; echo "Creating fresh iPhone 17 iOS 26.5..."; NEW=$(xcrun simctl create "iPhone 17 iOS 26.5" com.apple.CoreSimulator.SimDeviceType.iPhone-17 com.apple.CoreSimulator.SimRuntime.iOS-26-5); echo "NEW_SIMULATOR_ID=$NEW"; xcrun simctl boot "$NEW"; open -a Simulator; echo "Waiting..."; sleep 5; flutter devices -v; echo "Run with:"; echo "flutter run -d $NEW"'
